import { Injectable } from '@angular/core';
import { Scene } from 'three';

@Injectable({
  providedIn: 'root'
})
export class BuildingsService {
  scene:Scene;
  constructor() { }
  public init () {
    
  }
}
